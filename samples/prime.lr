def isPrime(x):
	if x == 2: return true
	elif x < 2 or x % 2 == 0: return false
	else:
		n = 3
		m = x ** 0.5

		while n < m:
			if x % n == 0: return false
			n += 2
		else: return true

value = 17
print(value, 'is ')

if (isPrime(value)):
	println('prime.')
else:
	println('not prime.')